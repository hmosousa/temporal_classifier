from typing import Literal

import datasets


def load_matres(
    split: Literal["train", "test"],
) -> datasets.Dataset:
    return
    # corpus = tieval.datasets.load_dataset("matres")
    # return data
